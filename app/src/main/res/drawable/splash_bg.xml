<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 底层白色 -->
    <item android:drawable="@android:color/white" />

    <!-- 顶层Logo居中 -->
    <item>
        <bitmap
            android:src="@drawable/splash_default_img"/>
    </item>
</layer-list>